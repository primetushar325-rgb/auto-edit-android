<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- INTERNET: used only by the mandatory update system (version.json + APK
         download). Frame extraction and all editing remain fully offline. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <!-- Android 13+ needs this to show the export progress notification. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <application android:theme="@style/AppTheme" android:label="Auto Edit" android:icon="@mipmap/ic_launcher" android:roundIcon="@mipmap/ic_launcher" android:allowBackup="true" android:usesCleartextTraffic="false">
        <!-- AdMob application id (required by play-services-ads). -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-7712127801733980~2067267819"/>
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".formula.CustomFormulaActivity" android:exported="false" />
        <activity android:name=".frames.FrameExtractorActivity" android:exported="false" />
        <activity android:name=".update.UpdateActivity" android:exported="false" android:excludeFromRecents="true" />
        <service android:name=".export.ExportService" android:exported="false" android:foregroundServiceType="dataSync" />
        <service android:name=".frames.FrameExtractService" android:exported="false" />
        <!-- Shared FileProvider: ZIP share/open (Frame Extractor) + APK install (update system). -->
        <provider
            android:name="com.autoedit.frames.ZipProvider"
            android:authorities="com.autoedit.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
